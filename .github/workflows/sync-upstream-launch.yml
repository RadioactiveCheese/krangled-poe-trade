name: Sync upstream (launch cadence)

on:
  schedule:
    - cron: '17 */4 * * *' # Every 4 hours, away from peak minute 0

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    uses: ./.github/workflows/sync-upstream.yml
